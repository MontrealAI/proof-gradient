name: Retargeted legacy Proof Gradient site workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy workflow is retired."
          echo "Use: Proof Gradient Command Center Root Authority v7.1."
