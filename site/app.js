/* Proof Gradient public site bootstrap.
 * Kept intentionally small so existing static pages do not depend on a backend
 * or external JavaScript framework.
 */
(function () {
  document.documentElement.classList.add('js-enabled');
}());
