/* Proof Gradient public site bootstrap.
 * Intentionally dependency-free: existing pages are static and generated server-side.
 */
(function () {
  document.documentElement.dataset.proofGradientSite = "static";
})();
