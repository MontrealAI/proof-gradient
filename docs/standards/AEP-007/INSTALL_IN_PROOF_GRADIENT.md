# Install AEP-007 in proof-gradient

Extract this package into:

`docs/standards/AEP-007/`

Copy the workflow:

`.github/workflows/add-aep007-public-safe-proof-report-standard-page.yml`

Then run:

Actions -> Add AEP-007 Public-Safe Proof Report Standard Website Page -> Run workflow

Use `deploy_pages = true`.
